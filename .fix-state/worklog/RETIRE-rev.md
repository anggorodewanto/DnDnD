---
bundle: RETIRE
reviewer: claude-opus-4-7 (read-only review pass)
date: 2026-05-11
tasks: A-08-retire-approved-transition, A-08-retire-created-via-schema, A-16-retire-approval-unreachable
verdict: APPROVE (with one process note)
---

# RETIRE bundle review

## Process note
`/.fix-state/worklog/RETIRE-impl.md` was NOT written by the implementer. Other
bundles in the worklog dir (DEATH-SAVE, DISPATCH-D, etc.) have impl notes; this
one is missing. Review proceeded on code + tests only.

## Per-task verdict

### A-08-retire-approved-transition — PASS
- `internal/registration/service.go:46-56` adds `approved -> retired` to
  `validTransitions`; `pending -> retired` retained.
- `internal/dashboard/approval_store.go:28-38` mirrors the map with
  `validApprovalTransitions["approved"]["retired"] = true`. Note: scope says
  `internal/character/service.go` but no such file exists; the transition logic
  lives in `registration` + `dashboard` packages — both updated.
- Coverage exercised in `approval_store_test.go` + new
  `registration/integration_test.go:TestIntegration_MarkPlayerCharacterRetireRequested_FlipsCreatedVia`.

### A-08-retire-created-via-schema — PASS
- New migration `db/migrations/20260511120000_extend_player_characters_created_via_retire.sql`
  drops + re-adds the CHECK with `'retire'` (idempotent, has Down).
- Historical migration `20260310120007_create_player_characters.sql` UNCHANGED (verified).
- `db/queries/player_characters.sql` adds `MarkPlayerCharacterRetireRequested`
  and `ListPlayerCharactersAwaitingApproval` (status=pending OR created_via=retire).
- `internal/refdata/player_characters.sql.go` regenerated by sqlc; signatures
  match SQL byte-for-byte (verified via grep against the const strings).
  `make test` runs `TestSqlcDriftCheck_PassesWhenClean` which would catch drift —
  it passes.

### A-16-retire-approval-unreachable — PASS
- `internal/discord/retire_handler.go` now calls
  `MarkPlayerCharacterRetireRequested` on `player_characters` (NOT
  `dm_queue_items`). Still uses dmqueue notifier for the DM ping; that is the
  correct notification channel, separate from the dashboard queue.
- `internal/dashboard/approval_handler.go:248` retire branch is reachable via
  the awaiting-approval list query; new test
  `TestApprove_RetireSubmission_TransitionsToRetired` exercises it end-to-end
  (calls `RetireCharacter`, updates card via `UpdateCardRetired`).
- pcStore wired in `cmd/dndnd/discord_handlers.go:373`.

## Verification
- `make build`: clean.
- `make test`: all packages PASS (no flaky AuthProtectedRoutes failure observed).
- `make cover-check`: OK, thresholds met (discord 87.22%, dashboard 94.29%,
  registration 93.41%, refdata 85.84%).
- `grep dm_queue_items.*retire`: no orphan callers.

## Next steps
- Optional: ask implementer to backfill `RETIRE-impl.md` for audit-trail
  consistency with other bundles.
- Update task front-matter (`status: open` -> `closed`) on the three task
  files, mirroring how prior bundles closed via `docs(.fix-state):` commits.
