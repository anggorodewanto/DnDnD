---
id: SR-039
title: DDB import advisories absent and homebrew tagging incomplete
severity: medium
source_batch: batch-12-leveling-creation-portal.md
source_section: SUMMARY.md §3 "DDB import advisories absent ... and homebrew tagging incomplete"
status: completed
attempts: 1
---

## Finding (verbatim from review)
DDB import advisories absent (e.g., "Wizard spell list includes Cure Wounds") and homebrew tagging incomplete.

## Acceptance criteria
- DDB import flow surfaces advisories for off-list / homebrew content (e.g., a Wizard with Cure Wounds).
- Items/spells from homebrew sources are tagged with a `homebrew` indicator on the character card / DM dashboard.
- Test: importing a DDB character with Cure Wounds on the Wizard spell list produces a visible advisory; the spell is tagged homebrew.
- Tests pass; `make cover-check` green.

## Worker plan
- Added failing tests for DM queue advisory text, approval detail API advisories, rendered character-sheet Homebrew/Off-list badges, and DDB inventory homebrew/source parsing.
- Surfaced DDB import warnings to the DM queue notification for `/import`.
- Added approval detail `advisories` derived from persisted DDB spell metadata and rendered them in the dashboard approval page.
- Rendered DDB spell `Homebrew` / `Off-list` indicators on the character sheet and preserved item source/homebrew tags into inventory display.
- Added DDB inventory source/homebrew parsing from available DDB item definition fields (`isHomebrew`, `homebrew`, and source-name heuristic).
- Ran targeted tests, `go test ./...`, `make cover-check`, and `make sqlc-check`.

## Files touched
- `internal/ddbimport/parser.go` — adds DDB spell homebrew/off-list metadata and marks Wizard Cure Wounds as off-list.
- `internal/ddbimport/validator.go` — emits advisory warnings for off-list imported class spells.
- `internal/ddbimport/*_test.go` — covers Wizard Cure Wounds advisory, parser tagging, persisted character data, DDB inventory source/homebrew parsing, and parse helper call sites.
- `internal/character/types.go` — extends shared `DDBSpellEntry` with `homebrew` and `off_list` flags and inventory items with `source` / `homebrew`.
- `internal/discord/registration_handler.go`, `internal/discord/import_ddb_handler_test.go` — surfaces import warnings in DM queue notification and covers Wizard Cure Wounds visibility.
- `db/queries/player_characters.sql`, `internal/refdata/player_characters.sql.go` — includes `character_data` in approval detail query for advisory derivation.
- `internal/dashboard/approval.go`, `internal/dashboard/approval_store.go`, `internal/dashboard/approval_handler.go` — exposes approval `advisories` JSON and renders dashboard warning box.
- `internal/dashboard/approval_*_test.go` — covers approval detail advisory derivation/API/template surface.
- `internal/portal/character_sheet.go`, `internal/portal/character_sheet_store.go`, `internal/portal/character_sheet_handler.go` — carries and renders DDB homebrew/off-list spell tags and inventory source/homebrew tags.
- `internal/portal/character_sheet_*_test.go` — covers character sheet preservation and rendered DDB spell badges.
- `internal/charactercard/format.go`, `internal/charactercard/service.go` — shows a homebrew/off-list spell count on character cards.
- `internal/charactercard/format_test.go` — covers card badge formatting and DDB spell tag counting.

## Reviewer notes
- Acceptance criteria met: DDB parser tags Wizard Cure Wounds as homebrew/off-list (`internal/ddbimport/parser.go:371`), validator emits the Wizard + Cure Wounds advisory (`internal/ddbimport/validator.go:122`), import persists the tagged spell data (`internal/ddbimport/service.go:341`), and `/import` sends warnings into the DM queue notification (`internal/discord/registration_handler.go:198`, `internal/discord/registration_handler.go:311`).
- DM approval visibility is covered: approval detail now includes `advisories` (`internal/dashboard/approval.go:33`), derives them from `character_data` (`internal/dashboard/approval_store.go:104`, `internal/dashboard/approval_store.go:123`), renders them in the dashboard warning box (`internal/dashboard/approval_handler.go:484`), and has API/template tests for the surface (`internal/dashboard/approval_handler_test.go:169`, `internal/dashboard/approval_page_test.go:42`).
- Rendered character surfaces visibly show tags: portal spell rows render `Homebrew` and `Off-list` (`internal/portal/character_sheet_handler.go:405`), inventory rows render `Homebrew` and source (`internal/portal/character_sheet_handler.go:428`), and character cards show a homebrew/off-list spell count (`internal/charactercard/format.go:87`).
- DDB/imported inventory item tags are preserved where DDB exposes them: shared inventory items now carry `source` and `homebrew` (`internal/character/types.go:105`), parser maps `sourceName` plus `isHomebrew`/`homebrew`/source-name heuristic (`internal/ddbimport/parser.go:112`, `internal/ddbimport/parser.go:271`, `internal/ddbimport/parser.go:278`), and parser coverage asserts both fields (`internal/ddbimport/parser_test.go:377`).
- Tests cover the requested surfaces: DM queue advisory visibility (`internal/discord/import_ddb_handler_test.go:68`), rendered sheet badges (`internal/portal/character_sheet_handler_test.go:228`), approval detail advisories (`internal/dashboard/approval_store_test.go:167`), DDB parser/service spell tags (`internal/ddbimport/parser_test.go:262`, `internal/ddbimport/service_test.go:312`), and item tag parsing (`internal/ddbimport/parser_test.go:377`).
- sqlc query changes are consistent: `db/queries/player_characters.sql:54` adds `c.character_data`, generated `internal/refdata/player_characters.sql.go:191` matches the SQL, the row struct includes `CharacterData` (`internal/refdata/player_characters.sql.go:218`), and scan order includes it (`internal/refdata/player_characters.sql.go:245`); `make sqlc-check` passed.
- Regression grep found the new metadata is not dropped on the reviewed parse/validation/display/approval paths: parser -> persisted character_data -> approval detail -> portal store/template -> card count (`internal/ddbimport/parser.go:371`, `internal/ddbimport/service.go:341`, `internal/dashboard/approval_store.go:104`, `internal/portal/character_sheet_store.go:284`, `internal/charactercard/service.go:342`).
- CLAUDE.md checked: TDD/coverage rules apply; `go test ./...`, `make cover-check` (post-exclusion overall coverage 91.89%), and `make sqlc-check` all passed.
- VERDICT: approved
