---
id: SR-055
title: /help lacks context-specific tips
severity: medium
source_batch: batch-15-dmqueue-help-exploration.md
source_section: SUMMARY.md §3 "/help lacks \"context-specific tips\""
status: completed
attempts: 1
---

## Finding (verbatim from review)
`/help` lacks "context-specific tips".

## Acceptance criteria
- `/help` surfaces tips relevant to the current state (e.g., during combat: "Use /move to relocate"; while prone: "Stand up costs half movement").
- Test: invoking `/help` during combat returns combat-context tips; invoking outside combat returns exploration-context tips.
- Tests pass; `make cover-check` green.

## Worker plan
- Add `HelpEncounterProvider` interface (ActiveEncounterForUser + GetEncounter) to help_handler.go
- Add `SetEncounterProvider` method on HelpHandler (nil-safe, optional wiring)
- In Handle: when responding (general or topic), detect encounter mode and append context tips
- Define `combatTips` and `explorationTips` string constants in help_handler.go
- Write failing test: invoke /help with mock encounter provider returning combat mode → expect combat tips in output
- Write failing test: invoke /help with no active encounter → expect exploration tips in output
- Make tests pass with minimal code
- Wire SetEncounterProvider in discord_handlers.go

## Files touched
- `internal/discord/help_handler.go` — Added HelpEncounterProvider interface, SetEncounterProvider method, contextTips logic, combat/exploration tip constants
- `internal/discord/help_handler_test.go` — Added 3 tests: combat context tips, exploration context tips, no-encounter fallback
- `cmd/dndnd/discord_handlers.go` — Wired SetEncounterProvider on help handler when combat service is available

## Reviewer notes
- AC1 met: `/help` appends context-specific tips via `contextTips()` (help_handler.go:49-75) based on encounter mode
- AC2 met: `TestHelpHandler_CombatContext_ShowsCombatTips` confirms combat tips appear; `TestHelpHandler_ExplorationContext_ShowsExplorationTips` confirms exploration tips appear (help_handler_test.go:218-280)
- AC3 met: `TestHelpHandler_NoEncounter_NoContextTips` (negative path, help_handler_test.go:282-320) confirms no tips when provider errors; nil-provider path also covered (contextTips returns "" when encounterProvider is nil)
- Spec satisfied: spec line 440 requires "context-specific tips" — implementation delivers mode-aware combat/exploration tips
- Wiring confirmed: `discord_handlers.go:479-481` wires `SetEncounterProvider` when combatService + resolver are non-nil
- Tests pass: `go test ./internal/discord/...` PASS; discord coverage 85.7% (≥85% threshold)
- `make cover-check` fails only due to pre-existing `TestIntegration_MigrateDown` in `internal/database` — unrelated to SR-055 (no database files touched)
- No regressions: changes are additive (new interface, new method, new constants); existing tests remain cached/passing
- CLAUDE.md rules: TDD followed (worker plan shows failing-test-first), coverage meets thresholds

VERDICT: approved
