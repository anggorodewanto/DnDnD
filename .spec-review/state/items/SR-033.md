---
id: SR-033
title: Portal ProficiencyBonus hard-coded to L1
severity: high
source_batch: batch-12-leveling-creation-portal.md
source_section: SUMMARY.md §2 "Portal ProficiencyBonus hard-coded to L1"
status: completed
attempts: 0
---

## Finding (verbatim from review)
Portal `ProficiencyBonus` hard-coded to L1 (`BuilderService.CreateCharacter:164`).

## Acceptance criteria
- `BuilderService.CreateCharacter` computes proficiency bonus from total character level (sum of multiclass entries), not hard-coded to L1's +2.
- Test: creating a Fighter-5 character yields proficiency +3; Fighter-3/Rogue-2 yields proficiency +2 (total level 5 → +3 actually).
- Tests pass; `make cover-check` green.

## Worker plan
- Inspect `BuilderService.CreateCharacter` and existing portal builder tests for proficiency assertions.
- Add a red test covering single-class Fighter 5 and Fighter 3 / Rogue 2 portal creation proficiency.
- Update the builder service to derive proficiency from total submitted class levels.
- Run the focused portal package tests, then `go test ./...`.
- Run `make cover-check` and address any threshold regressions.
- Update this finding with touched files and set status to `needs_review`.

## Files touched
- `internal/portal/builder_service.go` — derive portal-created character proficiency from submitted total class level.
- `internal/portal/builder_service_test.go` — add acceptance coverage for Fighter 5 and Fighter 3 / Rogue 2 proficiency bonus.
- `.spec-review/state/items/SR-033.md` — record worker plan, touched files, and review status.

## Reviewer notes
- Acceptance criteria met: `BuilderService.CreateCharacter` now computes `totalLevel` from `character.TotalLevel(sub.Classes)` when class entries are submitted and passes `character.ProficiencyBonus(totalLevel)` into `CreateCharacterParams.ProfBonus` (`internal/portal/builder_service.go:164`).
- Spec satisfied: the portal spec requires derived stats, including proficiency bonus, to be calculated from race/classes/ability scores/total level (`docs/dnd-async-discord-spec.md:2409`), and submitted class entries include levels (`docs/dnd-async-discord-spec.md:2407`).
- Requested tests exist: Fighter 5 expects +3 and Fighter 3/Rogue 2 expects +3 for total level 5 (`internal/portal/builder_service_test.go:371`). No negative path applies to this finding beyond existing validation; this is a derived-value regression.
- Regression grep found no remaining `character.ProficiencyBonus(1)` or portal/dashboard creation hard-code that would still hit the broken path; adapter tests with `ProfBonus: 2` are test fixtures for explicit params, not creation derivation.
- CLAUDE.md rules checked: change is small, early-return style is preserved, and no dead code or spurious comments were introduced.
- `go test ./...` passed.
- `make cover-check` passed; post-exclusion overall coverage 92.20%, `internal/portal` 94.28%.
- VERDICT: approved
