---
id: SR-063
title: /dashboard/levelup legacy page + /api/levelup POST unauthenticated
severity: medium
source_batch: batch-14-ws-recovery-multi.md
source_section: SUMMARY.md §3 "/dashboard/levelup legacy page + /api/levelup POST unauthenticated"
related: [SR-001, SR-016]
status: completed
attempts: 1
---

## Finding (verbatim from review)
`/dashboard/levelup` legacy page + `/api/levelup` POST unauthenticated.

## Acceptance criteria
- Both `/dashboard/levelup` and `POST /api/levelup` are gated by the appropriate auth middleware (DM auth where it mutates DM-only data; player auth otherwise — confirm against spec).
- Test: unauthenticated request to either route returns 401/403.
- Test: authenticated DM (or eligible player) request succeeds as before.
- Tests pass; `make cover-check` green.

## Worker plan
- Wrap the existing `levelup.NewHandler(...).RegisterRoutes(router)` call in a `router.Group` with `dmAuthMw` (can't use `mountDMOnlyAPIs` because levelup depends on services constructed later).
- Add `TestLevelUpRoutes_NonDMReceives403` — table-driven test asserting 403 for all 6 levelup routes when middleware denies.
- Add `TestLevelUpRoutes_DMReachesHandler` — same routes with passthrough middleware, asserting handler is reached.
- Add levelup entries to integration test `TestRun_AuthProtectedRoutesRejectUnauthenticated`.
- Run `go test ./...` and `make cover-check`.

## Files touched
- `cmd/dndnd/main.go` — Wrapped `levelup.NewHandler(...).RegisterRoutes(router)` in a `router.Group` with `dmAuthMw`, gating all levelup routes (POST /api/levelup/*, GET /dashboard/levelup) behind DM auth.
- `cmd/dndnd/auth_dm_routes_test.go` — Added `TestLevelUpRoutes_NonDMReceives403` (6 sub-tests asserting 403 for non-DM) and `TestLevelUpRoutes_DMReachesHandler` (6 sub-tests asserting handler is reached when middleware passes through).
- `cmd/dndnd/auth_middleware_test.go` — Added "levelup post" and "levelup page" entries to the integration test `TestRun_AuthProtectedRoutesRejectUnauthenticated` proving unauthenticated requests return 401.

## Reviewer notes
- `cmd/dndnd/main.go:1145-1148`: levelup routes correctly wrapped in `router.Group` with `dmAuthMw`, gating all 6 routes (5 POST /api/levelup/*, 1 GET /dashboard/levelup) behind DM auth.
- `cmd/dndnd/auth_dm_routes_test.go:215-292`: `TestLevelUpRoutes_NonDMReceives403` (6 sub-tests) confirms 403 for non-DM; `TestLevelUpRoutes_DMReachesHandler` (6 sub-tests) confirms handler is reached with passthrough middleware. Both pass.
- `cmd/dndnd/auth_middleware_test.go:140-141`: Integration test `TestRun_AuthProtectedRoutesRejectUnauthenticated` includes "levelup post" and "levelup page" entries asserting 401 for unauthenticated requests.
- Spec alignment: spec line 63-65 requires DM dashboard authentication via Discord OAuth2 with DM user ID verification. Level-up mutates DM-only data; `dmAuthMw` chains session middleware + `RequireDM` — correct.
- `go test ./...`: all packages pass except pre-existing `TestIntegration_MigrateDown` in `internal/database` (unrelated to SR-063, confirmed by testing on prior commit).
- `make cover-check` (coverage_check script): passes — 91.49% overall, all packages ≥85%.
- Existing `internal/levelup` tests (30+ tests) all pass — no regressions.
- CLAUDE.md rules satisfied: tests written, coverage thresholds met.
- VERDICT: approved
