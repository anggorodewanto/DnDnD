---
id: SR-041
title: "ASI/Feat pending" character-card indicator missing
severity: medium
source_batch: batch-12-leveling-creation-portal.md
source_section: SUMMARY.md §3 "\"⏳ ASI/Feat pending\" character-card indicator missing"
related: [SR-007]
status: completed
attempts: 2
---

## Finding (verbatim from review)
"⏳ ASI/Feat pending" character-card indicator missing.

## Acceptance criteria
- Character cards display an "⏳ ASI/Feat pending" indicator when the character has earned an ASI/feat slot but not yet chosen.
- Indicator clears once the choice is made.
- Test: level a character into an ASI tier without choosing → indicator appears; make the choice → indicator clears.
- Tests pass; `make cover-check` green.

## Worker plan
- Inspect character-card formatting/service data flow and existing pending ASI store semantics.
- Add red tests for card output when an ASI/feat is pending and after the choice is cleared.
- Thread the minimal pending flag into the card data/render path.
- Retry: reproduce the reviewer-reported approval ordering bug through the DM approval handler path.
- Retry: delete durable pending ASI state before invoking the approval service refresh; restore the pending choice if the approval write fails.
- Retry 2: add regression for durable pending delete failure during DM approval pre-delete.
- Retry 2: preserve the in-memory pending approval state and leave DM approval controls untouched when pre-delete fails.
- Run the focused package tests, then `go test ./...`.
- Run `make cover-check` and address any threshold regressions.
- Update this item with touched files and set status to `needs_review`.

## Files touched
- `internal/charactercard/service_test.go` — added red/green service tests for pending ASI indicator display and clearing after choice approval.
- `internal/charactercard/service.go` — reads durable pending ASI state while building card data.
- `internal/charactercard/format.go` — renders `⏳ ASI/Feat pending` when card data marks the choice pending.
- `internal/discord/asi_handler_test.go` — added regression coverage for the real DM approval ordering path and for durable pre-delete failure preserving retryable pending approval state.
- `internal/discord/asi_handler.go` — deletes pending ASI state before calling the approval service/card refresh, preserves in-memory pending state if durable delete fails, and restores pending state on approval failure.
- `.spec-review/state/items/SR-041.md` — recorded worker plan, touched files, and review status.

## Verification
- Red: `go test ./internal/discord -run TestASIHandler_HandleDMApprove_RemovesPendingBeforeApprovalRefresh -count=1` failed with stale `⏳ ASI/Feat pending`.
- Green targeted: `go test ./internal/discord -run 'TestASIHandler_HandleDMApprove|TestASIHandler_DeletesFromStoreOnApprove|TestASIHandler_HandleDMApprove_RemovesPendingBeforeApprovalRefresh' -count=1`
- Green targeted: `go test ./internal/levelup -count=1`
- Green targeted: `go test ./internal/charactercard -count=1`
- Green full: `go test ./...`
- Green coverage: `make cover-check` passed, post-exclusion overall coverage 91.81%.
- Retry 2 red: `go test ./internal/discord -run TestASIHandler_HandleDMApprove_DeletePendingErrorPreservesApprovalState -count=1` failed because the pending choice was removed from memory after durable delete failure.
- Retry 2 green targeted: `go test ./internal/discord -run 'TestASIHandler_HandleDMApprove_DeletePendingErrorPreservesApprovalState|TestASIHandler_HandleDMApprove_RemovesPendingBeforeApprovalRefresh|TestASIHandler_DeletesFromStoreOnApprove|TestASIHandler_HandleDMApprove_ApproveError' -count=1`
- Retry 2 green targeted: `go test ./internal/discord -count=1`
- Retry 2 green targeted: `go test ./internal/charactercard -count=1`
- Retry 2 green full: `go test ./...`
- Retry 2 green coverage: `make cover-check` passed, post-exclusion overall coverage 91.84%.

## Reviewer notes
- Acceptance criteria met: card data now checks durable pending ASI state in `internal/charactercard/service.go:230-269`, and `FormatCard` renders `⏳ ASI/Feat pending` from `ASIFeatPending` at `internal/charactercard/format.go:57-59`, matching the spec pending-state requirement in `docs/dnd-async-discord-spec.md:2507`.
- Indicator coverage is present: appears in `TestService_PostCharacterCard_ASITierPendingChoice_ShowsPendingIndicator` at `internal/charactercard/service_test.go:404-423`, and clears in `TestService_UpdateCard_ASIChoiceMade_ClearsPendingIndicator` at `internal/charactercard/service_test.go:426-443`.
- Real approval ordering is covered by `TestASIHandler_HandleDMApprove_RemovesPendingBeforeApprovalRefresh` at `internal/discord/asi_handler_test.go:1524-1577`; `HandleDMApprove` deletes pending state before `ApproveASI` at `internal/discord/asi_handler.go:490-498`, so the approval-triggered card refresh observes no pending row.
- Previous rejection is fixed: `removePendingChoice` returns before mutating memory when durable `Delete` fails at `internal/discord/asi_handler.go:326-337`, and the delete-failure branch edits only content without clearing components at `internal/discord/asi_handler.go:490-495`.
- Durable delete failure preservation is covered by `TestASIHandler_HandleDMApprove_DeletePendingErrorPreservesApprovalState` at `internal/discord/asi_handler_test.go:406-461`; it asserts approval is not called, the in-memory pending choice remains retryable, and DM controls are not cleared.
- Card creation/update regressions checked: `PostCharacterCard` and `UpdateCard` both route through `buildCardData` at `internal/charactercard/service.go:67` and `internal/charactercard/service.go:116`, while `OnCharacterUpdated` routes through `UpdateCard` at `internal/charactercard/service.go:141`.
- ASI approval paths checked: success removes pending before approval and clears controls after approval at `internal/discord/asi_handler.go:490-517`; approval service errors restore pending via `storePendingChoice` at `internal/discord/asi_handler.go:498-507`.
- `go test ./...` passed.
- `make cover-check` passed; post-exclusion overall coverage reported 91.84% and package thresholds were green.
- CLAUDE.md checked: TDD and coverage-gate requirements are satisfied by the recorded red/green retry notes and passing `make cover-check`.
- VERDICT: approved

## Worker notes (retry 2)
- `removePendingChoice` now calls durable `Delete` before mutating the in-memory pending map at `internal/discord/asi_handler.go:326-337`. If durable delete fails, the pending approval entry remains immediately retryable.
- The DM approval pre-delete failure branch now edits only the error content at `internal/discord/asi_handler.go:490-495`, leaving components unset so the existing approve/deny controls are not cleared as if approval proceeded.
- Regression coverage is `TestASIHandler_HandleDMApprove_DeletePendingErrorPreservesApprovalState` at `internal/discord/asi_handler_test.go:406-461`; it asserts approval is not called, pending state remains, the error is surfaced, and controls are preserved.
- The prior success-path ordering fix remains covered by `TestASIHandler_HandleDMApprove_RemovesPendingBeforeApprovalRefresh` at `internal/discord/asi_handler_test.go:1524-1577`.
- CLAUDE.md checked: TDD/coverage expectations considered; required coverage gate is green.
