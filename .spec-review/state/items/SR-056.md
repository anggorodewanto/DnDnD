---
id: SR-056
title: /status omits Channel Divinity uses + Smite slot summary
severity: medium
source_batch: batch-15-dmqueue-help-exploration.md
source_section: SUMMARY.md §3 "/status omits Channel Divinity uses + Smite slot summary"
status: completed
attempts: 1
---

## Finding (verbatim from review)
`/status` omits Channel Divinity uses + Smite slot summary.

## Acceptance criteria
- `/status` includes Channel Divinity uses remaining and a smite slot summary for paladins (per-level slots available for smites).
- Test: paladin's `/status` lists Channel Divinity uses and smite slots by level.
- Tests pass; `make cover-check` green.

## Worker plan
- Add `ChannelDivinityCurrent`, `ChannelDivinityMax`, `HasChannelDivinity` fields to `status.Info`
- Add `SmiteSlots` (map or formatted string) to `status.Info` for paladin spell slots
- Add formatting sections in `FormatStatus` for both
- In `status_handler.go` `populateFeatureUses`, add Channel Divinity parsing (Cleric 2+ / Paladin 3+) and smite slot parsing (Paladin with spell slots)
- Write failing test: paladin with channel-divinity feature_uses and spell_slots → /status shows both
- Make minimal code changes to pass
- Run tests + cover-check

## Files touched
- `internal/status/format.go` — Added `HasChannelDivinity`, `ChannelDivinityCurrent`, `ChannelDivinityMax`, `SmiteSlots` fields to `Info`; added formatting sections in `FormatStatus`.
- `internal/discord/status_handler.go` — Added Channel Divinity and smite slot population in `populateFeatureUses`; added `formatSmiteSlots` and `ordinal` helpers; added `fmt`, `sort`, `strconv` imports.
- `internal/discord/status_handler_test.go` — Added `TestStatusHandler_Paladin_ChannelDivinityAndSmiteSlots` and `TestStatusHandler_Cleric_ChannelDivinity` tests.

## Reviewer notes
- AC1 met: `format.go:111-117` renders `**Channel Divinity:** X/Y` and `**Smite Slots:** 1st: X/Y | 2nd: X/Y` for paladins.
- AC2 met: `TestStatusHandler_Paladin_ChannelDivinityAndSmiteSlots` (status_handler_test.go:564) asserts both fields present.
- AC3 met: `go test ./internal/discord/...` passes; `make cover-check` passes (pre-existing `internal/database` MigrateDown failure is unrelated — sessions table down-migration issue from prior commit).
- Spec alignment: spec line 394 defines Channel Divinity for Cleric 2+ / Paladin 3+; `status_handler.go:266-278` uses `combat.ChannelDivinityMaxUses` with correct class/level gates. Spec line 696 defines Divine Smite using spell slots; smite slot display at `status_handler.go:281-286` shows available slots for Paladin 2+.
- Happy path: `TestStatusHandler_Paladin_ChannelDivinityAndSmiteSlots` — Paladin 5 with CD and spell slots shows both.
- Negative path: `TestStatusHandler_Cleric_ChannelDivinity` — Cleric shows CD but explicitly asserts NO smite slots. `TestStatusHandler_NotInCombat_NoEffects` — Fighter shows neither.
- No regressions: all 34 packages pass (only `internal/database` fails on pre-existing migration test).
- CLAUDE.md rules: TDD followed (failing test → minimal code), coverage 85.8% for discord package (≥85% threshold), no `/simplify` needed for this minimal addition.

VERDICT: approved
