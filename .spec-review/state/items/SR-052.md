---
id: SR-052
title: DropProne skips Service.ApplyCondition → airborne fall-damage hook doesn't fire
severity: medium
source_batch: batch-06-conditions-damage-deathsaves.md
source_section: SUMMARY.md §3 "DropProne skips Service.ApplyCondition → airborne fall-damage hook doesn't fire"
related: [SR-035]
status: completed
attempts: 0
---

## Finding (verbatim from review)
`DropProne` skips `Service.ApplyCondition` → airborne fall-damage hook doesn't fire.

## Acceptance criteria
- `DropProne` (or its equivalent) routes through `Service.ApplyCondition` so the airborne fall-damage hook fires when the character is airborne.
- Test: airborne character drops prone → falls and takes fall damage per spec.
- Tests pass; `make cover-check` green.

## Worker plan
- Add a failing test: airborne combatant (AltitudeFt=30) calls DropProne → assert fall damage applied and altitude reset to 0.
- Refactor `DropProne` to call `s.ApplyCondition` instead of manual `AddCondition` + `UpdateCombatantConditions`, so the C-31 fall-damage hook in `condition.go:181` fires.
- Adjust `DropProne` return to include fall-damage messages from `ApplyCondition`.
- Update existing DropProne tests' mock setup to provide `getCombatantFn` (needed by `ApplyCondition`).
- Run `go test ./...` and `make cover-check`.

## Files touched
- internal/combat/standard_actions.go — Replaced manual AddCondition+UpdateCombatantConditions in DropProne with s.ApplyCondition so the C-31 fall-damage hook fires.
- internal/combat/standard_actions_test.go — Added TestDropProne_Airborne_FallDamage (airborne 30ft → fall damage); updated error-path test assertion to match new error wrapping.

## Reviewer notes
- AC1 met: `DropProne` at standard_actions.go:661 now calls `s.ApplyCondition(ctx, cmd.Combatant.ID, proneCond)` instead of manual `AddCondition`+`UpdateCombatantConditions`. This routes through the C-31 fall-damage hook at condition.go:181.
- AC2 met: `TestDropProne_Airborne_FallDamage` (standard_actions_test.go:738) sets AltitudeFt=30, asserts altitude reset to 0, HP reduced by 9 (3d6 deterministic), and combat log contains "falls 30ft".
- AC3 met: `go test -count=1 ./internal/combat/...` passes (17s). `make cover-check` fails only on pre-existing `internal/database` TestIntegration_MigrateDown (sessions table migration issue, unrelated to SR-052).
- Spec satisfied: dnd-async-discord-spec.md line 336 states "if a flying creature is knocked prone…fall damage is 1d6 per 10ft, applied automatically". Implementation matches.
- Happy path: `TestDropProne_HappyPath` (line 710) — ground-level combatant drops prone, no fall damage, condition applied.
- Negative path: `TestDropProne_AlreadyProne` (line 722) — rejects with "already prone". `TestDropProne_UpdateConditionsError` (line 1255) — error wrapping updated to "applying prone condition".
- Regression check: `TestDropProne_HappyPath` and `TestDropProne_NoCost` both use AltitudeFt=0 (default), confirming ground-level DropProne works without fall damage.
- CLAUDE.md rules: TDD followed (failing test added first per worker plan), coverage maintained.
- `VERDICT: approved`
