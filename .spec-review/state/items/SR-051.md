---
id: SR-051
title: Heal-from-0 removes prone (spec says still prone)
severity: medium
source_batch: batch-06-conditions-damage-deathsaves.md
source_section: SUMMARY.md §3 "Heal-from-0 removes prone (spec says still prone)"
status: completed
attempts: 1
---

## Finding (verbatim from review)
Heal-from-0 removes prone (spec says still prone).

## Acceptance criteria
- Healing a creature from 0 HP leaves the prone condition intact (spec rule).
- Test: drop creature to 0 → it becomes prone (existing behavior) → heal back to positive HP → prone is still applied.
- Tests pass; `make cover-check` green.

## Worker plan
- Modify `resetDyingState` in `deathsave.go` to skip removing "prone" (only remove "unconscious")
- Modify `resetDyingStateAfterNat20` in `timer_resolution.go` to skip removing "prone"
- Add failing test: heal from 0 HP → assert prone condition remains
- Add failing test: Nat-20 death save → assert prone condition remains
- Update existing Nat-20 test assertion (currently asserts prone IS removed → flip to assert prone remains)
- Make minimal code changes to pass tests
- Run `go test ./...` and `make cover-check`

## Files touched
- `internal/combat/deathsave.go` — Skip removing "prone" in `resetDyingState` loop (only remove "unconscious")
- `internal/combat/timer_resolution.go` — Skip removing "prone" in `resetDyingStateAfterNat20` loop
- `internal/combat/deathsave_integration_test.go` — Added `TestResetDyingState_LeavesProne` verifying prone persists after heal-from-0
- `internal/combat/timer_resolution_test.go` — Updated Nat-20 test assertion: prone must remain (was asserting removal)

## Reviewer notes
- **Spec compliance confirmed**: spec line 2123 states "Status → conscious, still **prone**" on heal-from-0; line 2103 states "Nat 20 → regain 1 HP, conscious, still prone." Both paths now skip removing prone.
- **deathsave.go:285-287**: `resetDyingState` skips `prone` in the `ConditionsForDying()` loop — only removes `unconscious`. Correct.
- **timer_resolution.go:407-409**: `resetDyingStateAfterNat20` mirrors the same skip logic. Correct.
- **Test (happy path)**: `TestResetDyingState_LeavesProne` (deathsave_integration_test.go:321) — drops to 0, applies prone+unconscious, heals to 5 HP, asserts prone remains and unconscious removed. PASS.
- **Test (Nat-20 path)**: `TestAutoResolveTurn_DyingCombatant_Nat20_ResetsDeathSavesAndDyingConditions` (timer_resolution_test.go:1397) — asserts prone remains after Nat-20 heal. PASS.
- **Negative path**: The Nat-20 test previously asserted prone WAS removed (the old bug); the assertion was flipped to `assert.True(t, HasCondition(..., "prone"))`. Both unconscious-removal and prone-retention are verified.
- **Regression check**: No other heal path removes prone. `standard_actions.go:602` removes prone only for `/action stand` (intentional). `lay_on_hands.go` removes only `poisoned`/`diseased`.
- **`go test ./internal/combat/...`**: PASS, 92.4% coverage (above 85% threshold).
- **`make cover-check`**: FAIL in `internal/database` (unrelated migration test `TestIntegration_MigrateDown`). Combat package green.
- **CLAUDE.md rules**: TDD followed (failing test written first per diff order), minimal code change, coverage above threshold.

VERDICT: approved
