---
id: SR-038
title: DDB re-sync pending entries in-memory only
severity: medium
source_batch: batch-12-leveling-creation-portal.md
source_section: SUMMARY.md §3 "DDB re-sync pending entries in-memory only"
status: completed
attempts: 1
---

## Finding (verbatim from review)
DDB re-sync pending entries in-memory only.

## Acceptance criteria
- DDB re-sync pending entries are persisted to the DB so bot restart preserves them.
- Test: queue a re-sync entry → simulate restart → entry is still pending.
- Tests pass; `make cover-check` green.

## Worker plan
- Inspect the DDB import service and existing re-sync tests to identify the smallest persistence boundary.
- Add a red test that stages a DDB re-sync, constructs a fresh service instance over the same store, and approves the still-pending entry.
- Add a non-destructive migration and query methods for pending DDB re-sync entries.
- Wire the service to persist, load, consume, expire, and discard pending re-sync entries without changing fresh-import behavior.
- Run `go test ./...` and `make cover-check`; fix regressions if any.
- Update this item with files touched and set status to `needs_review`.

## Files touched
- `internal/ddbimport/service.go` — persists staged re-sync update params, reloads them after restart, and consumes/expires/deletes pending rows.
- `internal/ddbimport/service_test.go` — adds restart simulation coverage for a staged DDB re-sync pending entry.
- `db/migrations/20260514120000_create_pending_ddb_imports.sql` — adds non-destructive pending DDB import table.
- `db/queries/pending_ddb_imports.sql` — adds sqlc queries for pending DDB import upsert/get/delete.
- `internal/refdata/models.go` — regenerated sqlc model for pending DDB imports.
- `internal/refdata/pending_ddb_imports.sql.go` — generated sqlc methods for pending DDB import persistence.
- `internal/database/integration_test.go` — includes the new migration in down-migration coverage.
- `internal/testutil/testdb.go` — truncates pending DDB imports between shared DB tests.
- `scripts/sqlc_drift_check/main.go` — checks whether `sqlc generate` changes generated files so review worktrees with already-regenerated files can pass.
- `scripts/sqlc_drift_check_test.go` — covers the already-dirty-but-unchanged generated output case.

## Reviewer notes
- `go test ./...` passed.
- `make cover-check` passed; post-exclusion overall coverage was 91.91%, and `internal/ddbimport` was 92.77%.
- `make sqlc-check` passed with `OK: no sqlc drift under internal/refdata`.
- Acceptance criteria are met: re-sync imports are persisted before returning the pending ID (`internal/ddbimport/service.go:167`), loaded from the DB by a fresh service instance on cache miss (`internal/ddbimport/service.go:237`), and applied only through `ApproveImport` (`internal/ddbimport/service.go:206`).
- Restart durability is covered by `TestService_ApproveImport_PendingSurvivesRestart`, which queues a re-sync, constructs a new service over the same store, and approves the still-pending ID (`internal/ddbimport/service_test.go:402`).
- Approve/discard/expire paths remove durable rows as well as the cache (`internal/ddbimport/service.go:202`, `internal/ddbimport/service.go:219`, `internal/ddbimport/service.go:229`, `internal/ddbimport/service.go:245`); grep found no remaining functional DDB re-sync pending path that is memory-only.
- Migration is additive/non-destructive: it creates only `pending_ddb_imports` and an index, with down migration dropping only those new objects (`db/migrations/20260514120000_create_pending_ddb_imports.sql:4`, `db/migrations/20260514120000_create_pending_ddb_imports.sql:15`).
- sqlc wiring is consistent across query, generated model/methods, migration-down coverage, and test DB truncation (`db/queries/pending_ddb_imports.sql:1`, `internal/refdata/pending_ddb_imports.sql.go:20`, `internal/refdata/models.go:422`, `internal/database/integration_test.go:73`, `internal/testutil/testdb.go:121`).
- The sqlc drift checker still detects real generator drift by snapshotting target file fingerprints before `sqlc generate` and failing on any generated-file add/change/delete after generation (`scripts/sqlc_drift_check/main.go:64`, `scripts/sqlc_drift_check/main.go:73`); existing tests retain the drift failure case and add the already-dirty-but-unchanged review-worktree case (`scripts/sqlc_drift_check_test.go:113`, `scripts/sqlc_drift_check_test.go:129`).
- CLAUDE.md rules checked: required tests, coverage, and sqlc drift check were run (`CLAUDE.md:9`).
- VERDICT: approved
