---
id: SR-060
title: Dead code — RegisterCommands delete-loop after bulk-overwrite
severity: medium
source_batch: batch-01-foundation.md
source_section: SUMMARY.md §3 "Dead code: RegisterCommands delete-loop after bulk-overwrite"
status: completed
attempts: 1
---

## Finding (verbatim from review)
Dead code: `RegisterCommands` delete-loop after bulk-overwrite.

## Acceptance criteria
- The unreachable delete-loop following the bulk-overwrite in `RegisterCommands` is removed (or, if defensible, justified with a comment + test exercising the path).
- No behavior change at registration; existing tests still pass.
- Tests pass; `make cover-check` green.

## Worker plan
- Locate `RegisterCommands` in `internal/discord/commands.go` (line 633).
- Remove the pre-fetch of existing commands (`ApplicationCommands` call).
- Remove the `currentNames` map construction.
- Remove the delete-loop that iterates `existing` and calls `ApplicationCommandDelete`.
- Keep only the `CommandDefinitions()` + `ApplicationCommandBulkOverwrite` call (which already atomically replaces all commands).
- Remove tests that exercise the now-deleted delete-loop path (`TestRegisterCommands_DeletesStaleCommands`, `TestRegisterCommands_NoDeleteForCurrentCommands`, `TestRegisterCommands_DeleteError`).
- Run `go test ./...` and `make cover-check` to confirm green.

## Files touched
- `internal/discord/commands.go` — Removed dead code: pre-fetch of existing commands, currentNames map, and delete-loop from RegisterCommands.
- `internal/discord/commands_test.go` — Removed TestRegisterCommands_DeletesStaleCommands and TestRegisterCommands_NoDeleteForCurrentCommands (tested dead code paths).
- `internal/discord/edge_cases_test.go` — Removed TestRegisterCommands_FetchError and TestRegisterCommands_DeleteError (tested dead code paths).

## Reviewer notes
- Dead delete-loop removed: `commands.go` diff shows removal of `ApplicationCommands` fetch, `currentNames` map, and delete-loop (lines 632–660 old). Only `CommandDefinitions()` + `ApplicationCommandBulkOverwrite` remain (commands.go:635–641).
- Comment updated to document that bulk-overwrite atomically removes stale commands (commands.go:633–634).
- No behavior change: `RegisterCommands` still calls `ApplicationCommandBulkOverwrite` with the same definitions; the removed code was unreachable dead logic since bulk-overwrite already handles stale removal.
- Tests removed: `TestRegisterCommands_DeletesStaleCommands`, `TestRegisterCommands_NoDeleteForCurrentCommands` (commands_test.go), `TestRegisterCommands_FetchError`, `TestRegisterCommands_DeleteError` (edge_cases_test.go) — all tested dead code paths.
- `go test ./internal/discord` passes (all tests green).
- `internal/discord` coverage: 85.8% (above 85% per-package threshold).
- `make cover-check` fails due to pre-existing `TestIntegration_MigrateDown` in `internal/database` (confirmed fails on stashed/clean state too — unrelated to this change).
- No unused imports or compilation issues in modified files.
- CLAUDE.md rules checked: no TDD violation (dead code removal, not new feature).

VERDICT: approved
