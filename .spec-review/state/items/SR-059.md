---
id: SR-059
title: Channel Divinity DM-routing for narrative subclass options has no DB row
severity: medium
source_batch: batch-07-features-classes.md
source_section: SUMMARY.md §3 "Channel Divinity DM-routing for narrative subclass options has no DB row"
related: [SR-002]
status: completed
attempts: 1
---

## Finding (verbatim from review)
Channel Divinity DM-routing for narrative subclass options has no DB row.

## Acceptance criteria
- Narrative Channel Divinity subclass options (e.g., specific Cleric domain features) insert a real `dm_queue_items` row so the DM sees the request.
- Insert-then-post ordering (matches SR-002).
- Test: invoking a narrative Channel Divinity option creates a queue row visible to the dashboard.
- Tests pass; `make cover-check` green.

## Worker plan
- Add `KindChannelDivinity` event kind to `internal/dmqueue/format.go` with label entry.
- Add `GuildID` and `CampaignID` fields to `ChannelDivinityDMQueueCommand`.
- Add `DMQueueItemID` field to `DMQueueResult`.
- In `ChannelDivinityDMQueue`, call `s.dmNotifier.Post(...)` (insert-then-post pattern from SR-002) after deducting use/action, before returning.
- Write a failing test: wire a `fakeDMNotifier`, invoke `ChannelDivinityDMQueue`, assert `notifier.posts` has 1 entry with correct kind/player/summary.
- Update Discord handler to pass `GuildID`/`CampaignID` (from `interaction.GuildID` / `encounter.CampaignID`).
- Run `go test ./...` and `make cover-check`.

## Files touched
- `internal/dmqueue/format.go` — Added `KindChannelDivinity` event kind and label entry.
- `internal/combat/channel_divinity.go` — Added `GuildID`/`CampaignID` to command, `DMQueueItemID` to result, added `postChannelDivinityToDMQueue` helper that calls `dmNotifier.Post`.
- `internal/combat/channel_divinity_test.go` — Added `TestChannelDivinityDMQueue_PostsDMQueueItem` verifying notifier receives correct event.
- `internal/discord/action_handler.go` — Threaded `GuildID`/`CampaignID` into the `ChannelDivinityDMQueueCommand` at the dispatch site.

## Reviewer notes
- `KindChannelDivinity` added to `internal/dmqueue/format.go:39` with label entry at line 99. Correct emoji (✝️) and `useColon: true`.
- `GuildID`/`CampaignID` fields added to `ChannelDivinityDMQueueCommand` (`channel_divinity.go:513-514`).
- `DMQueueItemID` field added to `DMQueueResult` (`channel_divinity.go:343`).
- `postChannelDivinityToDMQueue` helper (`channel_divinity.go:571-583`) calls `s.dmNotifier.Post(ctx, ...)` which follows insert-then-post ordering (notifier.go:172 inserts, then :176 sends). Matches SR-002 pattern.
- Discord handler threads `interaction.GuildID` and `encounter.CampaignID.String()` into the command (`action_handler.go:1015-1016`).
- Happy-path test `TestChannelDivinityDMQueue_PostsDMQueueItem` (channel_divinity_test.go:615) wires `fakeDMNotifier`, invokes `ChannelDivinityDMQueue`, asserts notifier received 1 post with correct Kind/PlayerName/Summary/GuildID/CampaignID, and verifies returned `DMQueueItemID`.
- Negative-path tests present: `TestChannelDivinityDMQueue_ActionAlreadyUsed`, `_GetCharacterError`, `_ParseFeatureUsesError`, `_ValidateChannelDivinityError`, `_DeductFeatureUseError`, `_UpdateTurnActionsError`, `_NotACharacter`.
- `go test ./internal/combat/...` passes (all 9 DMQueue tests green).
- Coverage check passes: overall 91.49%, combat package 92.45% — both above thresholds (90/85).
- `make cover-check` fails only due to pre-existing `TestIntegration_MigrateDown` in `internal/database` (confirmed fails on base commit without SR-059 changes). Not a regression.
- No regressions: all other packages pass (cached).
- CLAUDE.md rules satisfied: TDD cycle visible in test naming, coverage thresholds met.
- Spec satisfied: spec §Channel Divinity states DM-resolved options "route to `#dm-queue`" — implementation inserts a `dm_queue_items` row via the Notifier framework.

VERDICT: approved
