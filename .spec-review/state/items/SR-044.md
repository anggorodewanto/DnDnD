---
id: SR-044
title: requires_sight for teleport spells unenforced
severity: medium
source_batch: batch-09-spells.md
source_section: SUMMARY.md §3 "requires_sight for teleport spells unenforced"
status: completed
attempts: 2
---

## Finding (verbatim from review)
`requires_sight` for teleport spells unenforced.

## Acceptance criteria
- Teleport spells with `requires_sight=true` reject the cast when the caster cannot see the target tile (FoW / behind walls).
- Test: teleport spell targeting a tile outside vision is rejected with the spec'd error.
- Test: same spell with line-of-sight succeeds.
- Tests pass; `make cover-check` green.

## Worker plan
- Locate existing teleport casting tests and the visibility/cover helpers used for line-of-sight decisions.
- Add a red test for a `requires_sight=true` teleport rejected when the destination is hidden by wall/FoW, asserting the spec error.
- Add a positive test for the same spell succeeding when the destination has line of sight.
- Wire the smallest sight check into teleport validation without changing teleport path-validation behavior.
- Run focused package tests, then `go test ./...` and `make cover-check`.
- Update files touched and leave the item at `needs_review`.

## Files touched
- `internal/combat/teleport.go` — `ValidateTeleportDestinationWithSight` now fails closed when `requires_sight=true` but no sight context (Walls/FogOfWar) is supplied.
- `internal/combat/teleport_test.go` — added FoW rejection test (`TestCast_TeleportRequiresSightRejectsDestinationNotVisibleInFoW`); fixed success test to supply explicit visible FogOfWar + non-blocking Walls; fixed `TestCast_TeleportSelf` and `TestCast_TeleportWithAdditionalEffects` to supply sight context.
- `internal/discord/cast_handler.go` — `dispatchSingleTarget` now loads Walls and FogOfWar for teleport spells with `requires_sight=true`; added `loadCasterFogOfWar` helper.

## Reviewer notes
- AC1 (reject when caster cannot see): `ValidateTeleportDestinationWithSight` (teleport.go:99-127) checks FoW visibility and wall full-cover; fails closed when no sight context supplied. Correct.
- AC2 (negative test — FoW): `TestCast_TeleportRequiresSightRejectsDestinationNotVisibleInFoW` (teleport_test.go:~660) asserts exact error string. ✓
- AC2b (negative test — wall): `TestCast_TeleportRequiresSightRejectsDestinationBehindWall` (teleport_test.go:~621) asserts full-cover rejection. ✓
- AC3 (positive test): `TestCast_TeleportRequiresSightAllowsDestinationWithLineOfSight` (teleport_test.go:~700) confirms cast succeeds and position updates. ✓
- AC4 (tests pass): `go test ./...` all green; `make cover-check` → "OK: coverage thresholds met". ✓
- Spec ref: spec line 1041 (`teleport.requires_sight`) enforced via the new validator + handler wiring at cast_handler.go:360-367.
- Regression check: only caller of `ValidateTeleportDestinationWithSight` is spellcasting.go:1303 which reads `cmd.Walls`/`cmd.FogOfWar` — populated by cast_handler.go for `requires_sight=true` spells, unused (early-return) for `requires_sight=false` spells (e.g. Dimension Door). No broken paths.
- Existing tests (`TestCast_TeleportSelf`, `TestCast_TeleportWithAdditionalEffects`) updated to supply sight context — no silent regressions.
- CLAUDE.md compliance: early-return style throughout new code; no dead code; no over-engineering (single function + one handler helper).

VERDICT: approved
