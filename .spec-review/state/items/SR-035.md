---
id: SR-035
title: /fly "loses fly speed" fall-damage trigger missing
severity: high
source_batch: batch-05-movement-attack.md
source_section: SUMMARY.md §2 "/fly \"loses fly speed\" fall-damage trigger missing"
status: completed
attempts: 0
---

## Finding (verbatim from review)
`/fly` "loses fly speed" fall-damage trigger missing. Only prone-while-airborne is wired. Spell expiration, polymorph, dispel → silent float.

## Acceptance criteria
- Losing fly speed mid-air (spell expiration, polymorph end, dispel) triggers the fall pipeline → fall damage per spec (1d6 per 10 ft).
- Test: caster's Fly spell expires while airborne → falls and takes the correct damage.
- Test: polymorph into a non-flying form while airborne → falls and takes damage.
- Tests pass; `make cover-check` green.

## Worker plan
- Confirm spec language for flying/falling and existing fall-damage helper behavior.
- Add red tests for concentration cleanup ending Fly on an airborne target.
- Add red test for polymorph cleanup into a non-flying form while airborne.
- Reuse the existing fall pipeline so altitude reset, damage, and downstream damage hooks stay consistent.
- Keep the change scoped to combat service cleanup paths; avoid new abstractions unless tests require them.
- Run targeted combat tests, then `go test ./...` and `make cover-check`.
- Update this item with files touched and set status to `needs_review`.

## Files touched
- internal/combat/altitude.go — added fly/polymorph mechanical tags and shared the existing fall pipeline.
- internal/combat/concentration.go — triggers fall damage when Fly spell-sourced speed is removed during cleanup/expiration/dispel.
- internal/combat/condition.go — triggers fall damage when polymorph applies a non-flying form mid-air.
- internal/combat/spellcasting.go — records Fly's granted speed as a spell-sourced condition for later cleanup.
- internal/combat/concentration_test.go — added Fly expiration and non-flying polymorph airborne fall tests.

## Reviewer notes
- Acceptance criteria met: losing Fly spell-sourced speed via `BreakConcentrationFully` now routes through `RemoveSpellSourcedConditions` and calls `applyFallDamageFromAltitude` when the removed condition ends fly speed (`internal/combat/concentration.go:170`, `internal/combat/concentration.go:210`, `internal/combat/altitude.go:146`).
- Spec satisfied: `docs/dnd-async-discord-spec.md:326` defines `/fly`, and `docs/dnd-async-discord-spec.md:336` requires automatic 1d6 per 10ft fall damage when a flying creature loses fly speed; the shared fall helper still resets altitude and uses the damage pipeline (`internal/combat/altitude.go:150`, `internal/combat/altitude.go:157`, `internal/combat/altitude.go:169`).
- Tests present and specific: Fly expiration test asserts a 30ft fall deals deterministic 3d6 damage, HP 30 -> 21 (`internal/combat/concentration_test.go:1026`); polymorph/non-flying negative path asserts a 20ft fall deals deterministic 2d6 damage, HP 24 -> 18 (`internal/combat/concentration_test.go:1084`).
- Regression grep checked concentration/dispel/manual drop paths: voluntary dashboard drop, failed concentration saves, new concentration replacement, rage cleanup, and combat-end cleanup all funnel through `BreakConcentrationFully` (`internal/combat/dm_dashboard_handler.go:103`, `internal/combat/concentration.go:467`, `internal/combat/concentration.go:591`, `internal/combat/rage.go:281`, `internal/combat/service.go:1122`). The raw clear for readied-spell expiry occurs before the readied spell effect is applied, so it is not a Fly-speed removal bypass (`internal/combat/initiative.go:796`).
- CLAUDE.md rules checked: changes are scoped, use existing early-return style, reuse the existing fall/damage pipeline, and do not introduce dead code or spurious comments.
- Verification: `go test ./...` passed; `make cover-check` passed with post-exclusion overall coverage 91.96% and `internal/combat` 92.63%.
- VERDICT: approved
