---
id: SR-015
title: DM-created multiclass dropped to level-1 single class
severity: critical
source_batch: batch-12-leveling-creation-portal.md
source_section: SUMMARY.md §1 item 15
status: completed
attempts: 1
---

## Finding (verbatim from review)
DM-created multiclass dropped to level-1 single class. `internal/dashboard/charcreate_service.go:71-79` maps only `primaryClass`/`primarySubclass` into `CreateCharacterParams`; `sub.Classes` never forwarded. `BuilderStoreAdapter.resolveClassEntries` falls back to a single L1 entry. Pre-approved status means it hits production without DM review.

## Acceptance criteria
- `charcreate_service.go` forwards the full `sub.Classes` slice (every class/subclass/level entry) into `CreateCharacterParams`.
- `BuilderStoreAdapter.resolveClassEntries` consumes the multiclass list rather than the L1 fallback.
- Test: DM creates a Fighter-3 / Rogue-2 character → resulting character has both class entries with correct levels and the appropriate proficiency bonus.
- Tests pass; `make cover-check` green.

## Worker plan
- `internal/dashboard/charcreate_service.go` already constructs
  `portal.CreateCharacterParams` but only forwards
  `primaryClass`/`primarySubclass`. `portal.CreateCharacterParams`
  already has a `Classes []character.ClassEntry` field, and
  `BuilderStoreAdapter.resolveClassEntries`
  (`internal/portal/builder_store_adapter.go:31-49`) already
  prefers `p.Classes` when non-empty. The fix is a single new
  field on the params struct literal.
- `DeriveDMStats` already computes the correct multiclass
  `ProficiencyBonus` via `character.TotalLevel(sub.Classes)` →
  `character.ProficiencyBonus(totalLevel)` and threads it into
  `charParams.ProfBonus`. No change needed there.
- Red: add a new table-driven test
  `TestDMCharCreateService_CreateCharacter_Multiclass_ForwardsClasses`
  (Fighter-3 / Rogue-2) asserting:
  - `store.lastCharParams.Classes` is the full slice
  - `store.lastCharParams.ProfBonus == 3` (total level 5 → +3)
  - HP matches Fighter 5/CON 12 expectation via existing helpers
- Green: add `Classes: sub.Classes,` to the `charParams` literal
  in `CreateCharacter`.
- Verify: existing `TestDMCharCreateService_CreateCharacter_Multiclass`
  continues to pass (it asserts only on `Class`/`Saves`).
- Run `go test ./...` + `make cover-check`.

## Files touched
- `internal/dashboard/charcreate_service.go` — forward `sub.Classes`
  into `portal.CreateCharacterParams.Classes`.
- `internal/dashboard/charcreate_service_test.go` — new red/green
  multiclass forwarding test.

## Reviewer notes
- Confirmed `Classes: sub.Classes` added at `internal/dashboard/charcreate_service.go:83` inside the `charParams` literal; diff is minimal (4 lines including comment) — no unrelated edits.
- `internal/portal/builder_store_adapter.go:31-49` (`resolveClassEntries`) verified by direct read: when `len(p.Classes) > 0` it iterates and emits filtered entries with `Level` floored at 1; only falls back to the L1 single-class entry when slice is empty/all-empty. Behavior matches plan.
- New test `TestDMCharCreateService_CreateCharacter_Multiclass_ForwardsClasses` (`internal/dashboard/charcreate_service_test.go:300-335`) asserts:
  - both Fighter (Champion, L3) and Rogue (Thief, L2) entries persisted in order with correct `Class`/`Subclass`/`Level`
  - `ProfBonus == 3` for total level 5 — guards against L1 fallback
- `go test ./internal/dashboard/...` for multiclass suite: PASS.
- `make cover-check`: PASS — `internal/dashboard` at 92.83%, all thresholds met.
- Aligns with CLAUDE.md: minimal single-field forward, no over-engineering, no scope creep.

VERDICT: approved
