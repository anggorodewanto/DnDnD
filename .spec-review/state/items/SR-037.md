---
id: SR-037
title: /bonus offhand surface mismatch
severity: high
source_batch: batch-05-movement-attack.md
source_section: SUMMARY.md §2 "/bonus offhand surface mismatch"
status: completed
attempts: 3
---

## Finding (verbatim from review)
`/bonus offhand` surface mismatch. Spec uses `/bonus offhand`; implementation uses `/attack offhand:true` (`attack_handler.go:170`). Service-side logic correct.

## Acceptance criteria
- A `/bonus offhand` Discord command exists and invokes the same service path currently handling `/attack offhand:true`.
- The legacy `/attack offhand:true` path remains functional (or is removed per spec — confirm with spec).
- Test: `/bonus offhand` consumes the bonus action, makes the off-hand attack, and applies the correct mod rules.
- Tests pass; `make cover-check` green.

## Worker plan
- Confirm the spec command surface for `/bonus offhand` and preserve legacy `/attack offhand:true`.
- Inspect current `/bonus` and `/attack` handler patterns for target parsing, turn gating, map/wall enrichment, and combat-log responses.
- Add a red test showing `/bonus offhand <target>` routes through `OffhandAttack` with the active turn, target, and off-hand damage result.
- Add red command-path coverage proving `/bonus offhand` forwards encounter wall context and rejects full cover before persisting bonus-action use.
- Add command-path coverage with a real `combat.Service` proving `/bonus offhand` consumes the bonus action and omits the ability modifier from off-hand damage without Two-Weapon Fighting style.
- Keep the existing `/attack offhand:true` test path green as the legacy surface.
- Implement the smallest `/bonus offhand` dispatch shim that reuses the existing off-hand service path and the same wall-loading context as `/attack`.
- Run focused Discord handler tests, then `go test ./...` and `make cover-check`.

## Files touched
- `cmd/dndnd/discord_handlers.go` — wired `handlers.bonus.SetMapProvider(deps.queries)` beside the existing attack map-provider wiring so production `/bonus offhand` receives wall/full-cover context.
- `cmd/dndnd/discord_handlers_wiring_test.go` — added a production wiring regression test that fails if the bonus handler is constructed without a map provider.
- `internal/discord/bonus_handler.go` — added `/bonus offhand <target>` dispatch to the existing `OffhandAttack` service path and now forwards parsed encounter map walls into `OffhandAttackCommand`, preserving `/attack offhand:true`.
- `internal/discord/bonus_handler_test.go` — added mock routing/wall-forwarding coverage plus real-service command-path tests for full-cover rejection before bonus-action persistence, bonus-action consumption, and off-hand damage without ability mod.
- `.spec-review/state/items/SR-037.md` — recorded retry plan, touched files, verification, and `needs_review` status.

## Retry verification
- Retry wiring check: `go test ./cmd/dndnd -run 'TestBuildDiscordHandlers_(AttackHandlerHasMapProvider|BonusHandlerHasMapProvider)'` passed.
- Focused bonus command check: `go test ./internal/discord -run 'TestBonusHandler_Offhand'` passed.
- `go test ./...` passed.
- `make cover-check` passed: overall post-exclusion coverage 91.94%; `internal/discord` 86.82%.
- Focused red/green check: `go test ./internal/discord -run 'TestBonusHandler_Offhand'` failed before implementation because `BonusHandler` had no `SetMapProvider`, then passed after wall forwarding was implemented.
- `go test ./...` passed.
- `make cover-check` passed: overall post-exclusion coverage 91.94%; `internal/discord` 86.84%.

## Reviewer notes
- Spec command surface is satisfied: `/bonus` is registered as an action+args command (`internal/discord/commands.go:205`), the spec names `/bonus offhand` (`docs/dnd-async-discord-spec.md:463`, `docs/dnd-async-discord-spec.md:728`), and `BonusHandler` dispatches `offhand`/`off-hand` to `OffhandAttack` (`internal/discord/bonus_handler.go:163`, `internal/discord/bonus_handler.go:273`).
- Production wiring now satisfies the last rejection: `attachCombatActionHandlers` calls `handlers.bonus.SetMapProvider(deps.queries)` (`cmd/dndnd/discord_handlers.go:620`), and `TestBuildDiscordHandlers_BonusHandlerHasMapProvider` would fail if the production bonus handler lacks a map provider (`cmd/dndnd/discord_handlers_wiring_test.go:52`).
- Wall/full-cover context now matches the legacy off-hand path: `/bonus offhand` loads parsed encounter walls into `OffhandAttackCommand.Walls` (`internal/discord/bonus_handler.go:113`, `internal/discord/bonus_handler.go:282`), while `/attack offhand:true` still loads and passes the same wall context (`internal/discord/attack_handler.go:230`, `internal/discord/attack_handler.go:232`).
- Command-path tests cover the requested behavior: routing to `OffhandAttack` (`internal/discord/bonus_handler_test.go:267`), wall forwarding (`internal/discord/bonus_handler_test.go:290`), bonus-action consumption and no off-hand damage ability modifier (`internal/discord/bonus_handler_test.go:377`), and full-cover rejection before bonus-action persistence (`internal/discord/bonus_handler_test.go:459`).
- Legacy `/attack offhand:true` remains functional: the option is still read (`internal/discord/attack_handler.go:170`) and existing coverage verifies it routes to the off-hand service path (`internal/discord/attack_handler_test.go:203`).
- `go test ./...` passed.
- `make cover-check` passed: overall post-exclusion coverage 91.94%; `internal/discord` 86.82%.
- CLAUDE.md check: required coverage thresholds passed; red/green TDD and `/simplify` cannot be independently verified from the final diff.
- VERDICT: approved
