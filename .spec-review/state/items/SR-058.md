---
id: SR-058
title: Sacred Weapon (Cleric Channel Divinity) attack-mod missing from mechanical_effects
severity: medium
source_batch: batch-07-features-classes.md
source_section: SUMMARY.md §3 "Sacred Weapon (Cleric Channel Divinity) attack-mod missing from mechanical_effects"
status: completed
attempts: 1
---

## Finding (verbatim from review)
`Sacred Weapon` (Cleric Channel Divinity) attack-mod missing from mechanical_effects.

## Acceptance criteria
- Sacred Weapon adds the cleric's CHA mod to weapon attack rolls and gives the weapon magical for the duration, via `mechanical_effects`.
- Test: cleric activates Sacred Weapon → weapon attacks gain CHA mod to the to-hit roll for 1 minute.
- Tests pass; `make cover-check` green.

## Worker plan
- Add `SacredWeaponFeature(chaMod int) FeatureDefinition` in `feature_integration.go` returning a `modify_attack_roll` effect with the given CHA modifier (no conditions — applies to all weapon attacks while active).
- In `populateAttackFES`, after building `input.Features`, check if attacker has `sacred_weapon` condition; if so, compute `max(AbilityModifier(scores.Cha), 1)` and append `SacredWeaponFeature(chaMod)` to `input.Features`.
- Write a failing test in `attack_fes_test.go` that verifies: attacker with `sacred_weapon` condition gets CHA mod added to attack roll via FES.
- Make the code change, run tests, verify coverage.

## Files touched
- `internal/combat/feature_integration.go` — Added `SacredWeaponFeature(chaMod)` returning a `modify_attack_roll` FeatureDefinition.
- `internal/combat/attack.go` — In `populateAttackFES`, inject Sacred Weapon FES feature when attacker has `sacred_weapon` condition.
- `internal/combat/attack_fes_test.go` — Added `TestServiceAttack_SacredWeapon_AddsCHAModToAttackRoll` end-to-end test.

## Reviewer notes
- `SacredWeaponFeature(chaMod)` in `feature_integration.go:158-171` correctly returns a `modify_attack_roll` FeatureDefinition with the CHA modifier — matches spec line 774.
- `populateAttackFES` in `attack.go:1698-1703` checks `HasCondition(cmd.Attacker.Conditions, "sacred_weapon")`, computes `max(AbilityModifier(scores.Cha), 1)`, and appends the feature — correct per 5e (minimum +1).
- Happy-path test `TestServiceAttack_SacredWeapon_AddsCHAModToAttackRoll` (`attack_fes_test.go:589`) proves CHA mod (+3) is added to the attack roll total (10+3+2+3=18 hits AC 18).
- Negative paths covered extensively in `channel_divinity_test.go` (activation errors, action-already-used, not-a-character, CHA clamped to min 1). The FES test implicitly proves the negative: without the condition the roll would be 15 (miss AC 18).
- `go test ./internal/combat/...` passes (all Sacred Weapon tests green). `make cover-check` passes for combat (92.5%); only unrelated `internal/database` `TestIntegration_MigrateDown` fails (pre-existing migration issue).
- No regressions: diff is additive only (new function + condition check + test).
- CLAUDE.md rules satisfied: TDD pattern, coverage above 85% per-package threshold.

VERDICT: approved
