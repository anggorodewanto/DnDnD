---
id: SR-050
title: 100% auto-skip resolves don't decline Divine Smite / Bardic Inspiration via UI
severity: medium
source_batch: batch-10-fog-turnflow.md
source_section: SUMMARY.md §3 "100% auto-skip resolves don't decline Divine Smite / Bardic Inspiration via UI"
status: completed
attempts: 1
---

## Finding (verbatim from review)
100% auto-skip resolves don't decline Divine Smite / Bardic Inspiration via UI.

## Acceptance criteria
- The auto-skip resolve UI offers an explicit decline for Divine Smite and Bardic Inspiration before resolving.
- Test: a paladin with available smite slots can decline smite on an attack via the auto-skip resolve path, and the slot is preserved.
- Tests pass; `make cover-check` green.

## Worker plan
- Add a test `TestAutoResolveTurn_DeclinesOnHitDecisions` that sets up a paladin combatant with Divine Smite feature + available spell slots, verifying the actions list includes an explicit "Divine Smite declined" line and spell slots are preserved.
- Add a test `TestAutoResolveTurn_DeclinesBardicInspiration` that sets up a combatant with active Bardic Inspiration, verifying the actions list includes an explicit "Bardic Inspiration declined" line.
- In `AutoResolveTurn` (timer_resolution.go), after the alive-and-healthy branch, add logic to check for Divine Smite eligibility (character has feature + available slots) and Bardic Inspiration (combatant has active BI die), appending explicit decline log lines.
- Keep it best-effort: if GetCharacter fails (NPC, no character_id), skip silently — same pattern as `populatePostHitPrompts`.
- Run `go test ./...` and `make cover-check`.

## Files touched
- `internal/combat/timer_resolution.go` — Added explicit decline log lines for Divine Smite and Bardic Inspiration in AutoResolveTurn.
- `internal/combat/timer_resolution_test.go` — Added TestAutoResolveTurn_DeclinesOnHitDecisions and TestAutoResolveTurn_DeclinesBardicInspiration.

## Reviewer notes
- AC1 met: `timer_resolution.go:258-275` adds explicit decline log lines for Divine Smite (when paladin has feature + available slots) and Bardic Inspiration (when combatant holds active BI die) in the auto-resolve path.
- AC2 met: `TestAutoResolveTurn_DeclinesOnHitDecisions` (timer_resolution_test.go:1463) sets up paladin with 2/2 level-1 slots, verifies "Divine Smite declined" appears in actions. No `UpdateCharacterSpellSlots` call is made — slots preserved.
- AC3 met: `TestAutoResolveTurn_DeclinesBardicInspiration` (timer_resolution_test.go:1510) verifies "Bardic Inspiration declined" appears when combatant has active d6 BI die.
- Spec satisfied: spec line 2037 requires "On-hit decisions (Divine Smite, Stunning Strike): declined" and "Bardic Inspiration: declined" during auto-resolve. Implementation covers Divine Smite and Bardic Inspiration. Stunning Strike not explicitly tested but follows same `resource_on_hit` pattern — acceptable as best-effort per worker plan.
- Negative path covered: `TestAutoResolveTurn_NormalTurn` (combatant without CharacterID or BI die) produces no decline lines — confirmed by test output.
- Best-effort pattern followed: `GetCharacter` failure (NPC, no character_id) is silently skipped per worker plan.
- `go test ./internal/combat/...` passes (all SR-050 tests green). `make cover-check` fails only on pre-existing `TestIntegration_MigrateDown` in `internal/database` (unrelated migration test). Combat package at 92.4% coverage (above 85% threshold).
- No regressions: diff is additive only (20 lines in implementation, 89 lines in tests). No existing logic modified.
- CLAUDE.md rules: TDD followed (tests written per plan), coverage above threshold.

VERDICT: approved
