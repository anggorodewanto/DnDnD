---
id: SR-048
title: /action grapple alias missing (only via /shove --mode grapple)
severity: medium
source_batch: batch-08-tactical-equipment.md
source_section: SUMMARY.md §3 "/action grapple alias missing (only via /shove --mode grapple)"
status: completed
attempts: 1
---

## Finding (verbatim from review)
`/action grapple` alias missing (only via `/shove --mode grapple`).

## Acceptance criteria
- A `/action grapple` Discord command exists and invokes the same service path as `/shove --mode grapple`.
- Test: `/action grapple` against a target performs the contested check and applies the grappled condition correctly.
- Tests pass; `make cover-check` green.

## Worker plan
- Add `Grapple` method to `ActionCombatService` interface in `action_handler.go`
- Add `"grapple"` to `isDispatchSubcommand` switch
- Add `case "grapple":` in `handleCombatSubcommand` switch, calling a new `dispatchGrapple` method
- Implement `dispatchGrapple` on `ActionHandler`: resolve target from args, call `combatService.Grapple`
- Add `grappleCalls`/`grappleResult` fields + `Grapple` method to `fakeActionCombatService` in test
- Write test `TestActionHandler_Dispatch_Grapple` verifying `/action grapple OS` dispatches to combat.Grapple
- Write test for missing target arg returning usage hint
- Run `go test ./...` and `make cover-check`

## Files touched
- `internal/discord/action_handler.go` — Added `Grapple` to `ActionCombatService` interface, `"grapple"` to `isDispatchSubcommand`, case in `handleCombatSubcommand`, and `dispatchGrapple` method
- `internal/discord/action_handler_test.go` — Added `grappleCalls`/`grappleResult`/`grappleErr` fields and `Grapple` method to `fakeActionCombatService`; updated unrelated test that used "grapple" as freeform text
- `internal/discord/action_handler_dispatch_test.go` — Added 3 tests: success dispatch, missing target, service error
- `cmd/dndnd/discord_handlers.go` — Added `Grapple` passthrough on `actionCombatServiceAdapter`

## Reviewer notes
- `Grapple` added to `ActionCombatService` interface at `action_handler.go:48` — matches the same `combat.GrappleCommand` + `*dice.Roller` signature used by `/shove --mode grapple` (`shove_handler.go:21`).
- `"grapple"` added to `isDispatchSubcommand` switch at `action_handler.go:305`.
- `case "grapple":` routes to `dispatchGrapple` at `action_handler.go:641`.
- `dispatchGrapple` (action_handler.go:892–924) resolves target from args, validates roller, and delegates to `combatService.Grapple` — same service path as `/shove --mode grapple`.
- Adapter passthrough added in `cmd/dndnd/discord_handlers.go:1546–1548`.
- Fake service updated in `action_handler_test.go:78–80,198–201` with `grappleCalls`/`grappleResult`/`grappleErr`.
- Existing test that used "grapple" as freeform text updated to "flip the table" (`action_handler_test.go:462`).
- Three tests added in `action_handler_dispatch_test.go:543–583`: happy path (`TestActionHandler_Dispatch_Grapple`), missing target (`_MissingTarget`), service error (`_ServiceError`).
- Spec line 377 (`/action grapple G1 — Grapple a target (contested Athletics check)`) is satisfied.
- `go test ./internal/discord/` passes (85.7% coverage). `go test ./cmd/dndnd/` passes.
- `make cover-check` fails only on pre-existing `internal/database` `TestIntegration_MigrateDown` (confirmed fails on HEAD without SR-048 changes).
- No regressions: existing `/action escape` and `/shove --mode grapple` tests still pass.
- CLAUDE.md rules followed: TDD pattern, coverage target met for affected packages.

VERDICT: approved
