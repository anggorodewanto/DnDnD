---
id: SR-048
title: /action grapple alias missing (only via /shove --mode grapple)
severity: medium
source_batch: batch-08-tactical-equipment.md
source_section: SUMMARY.md §3 "/action grapple alias missing (only via /shove --mode grapple)"
status: pending
attempts: 0
---

## Finding (verbatim from review)
`/action grapple` alias missing (only via `/shove --mode grapple`).

## Acceptance criteria
- A `/action grapple` Discord command exists and invokes the same service path as `/shove --mode grapple`.
- Test: `/action grapple` against a target performs the contested check and applies the grappled condition correctly.
- Tests pass; `make cover-check` green.

## Worker plan
- (filled in by implementer)

## Files touched
- (filled in by implementer)

## Reviewer notes
- (filled in by reviewer)
