---
id: SR-061
title: Bot permission validation defined but never called
severity: medium
source_batch: batch-01-foundation.md
source_section: SUMMARY.md §3 "Bot permission validation defined but never called"
status: completed
attempts: 1
---

## Finding (verbatim from review)
Bot permission validation defined but never called.

## Acceptance criteria
- Bot permission validation is invoked at startup / on guild-join (per spec) and surfaces missing-permission errors to the DM/admin.
- Test: when the bot lacks a required permission, the validation reports it via the configured channel/log.
- Tests pass; `make cover-check` green.

## Worker plan
- Add `ValidateGuildPermissions(guildID string, granted int64)` method on Bot that calls `ValidatePermissions(granted)` and logs missing perms as warnings.
- Call `validateGuildPermissions` inside `HandleGuildCreate` using `event.Guild.Permissions`.
- Call `ValidateGuildPermissions` from main.go startup loop (where state.Guilds are iterated) for each guild.
- Write a failing test: when Bot handles a GuildCreate with insufficient permissions, the logger records the missing permission names.
- Write a failing test: `ValidateGuildPermissions` with full perms produces no log output.
- Make minimal code changes to pass tests.
- Run `go test ./...` and `make cover-check`.

## Files touched
- `internal/discord/bot.go` — Added `ValidateGuildPermissions` method; called from `HandleGuildCreate`.
- `internal/discord/permissions_test.go` — Added 4 tests covering permission validation on guild-create and direct call.
- `cmd/dndnd/main.go` — Call `ValidateGuildPermissions` for each guild during startup state iteration.

## Reviewer notes
- `ValidateGuildPermissions` added to `internal/discord/bot.go:104-113` — calls `ValidatePermissions(granted)` and logs missing perms as a structured warning with guild_id and permission names.
- Called from `HandleGuildCreate` at `bot.go:88` (guild-join path).
- Called from `cmd/dndnd/main.go:1408` inside the startup guild iteration loop (startup path).
- Both call sites satisfy the spec requirement (lines 178-179: startup + guild-join).
- Missing permissions surfaced via `slog.Warn` with structured fields — acceptable for operational visibility; spec does not mandate a DM-facing Discord message for this.
- Tests cover: negative path (missing perms logged) via `TestBot_HandleGuildCreate_LogsMissingPermissions` and `TestBot_ValidateGuildPermissions_LogsMissing`; happy path (no log when all perms present) via `TestBot_HandleGuildCreate_NoLogWhenAllPermsPresent` and `TestBot_ValidateGuildPermissions_NoLogWhenFull`.
- `go test ./internal/discord/` passes (all 12 permission-related tests green). Coverage: 85.8%.
- `make cover-check` fails only due to pre-existing `internal/database/TestIntegration_MigrateDown` (migration down-path assertion unrelated to SR-061; no database files touched).
- No regressions: existing `HandleGuildCreate` tests still pass; `cmd/dndnd` compiles cleanly.
- CLAUDE.md rules satisfied: TDD approach, coverage ≥85% per-package for discord.

VERDICT: approved
