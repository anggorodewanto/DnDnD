---
id: SR-036
title: /dashboard/exploration/transition-to-combat doesn't transition
severity: high
source_batch: batch-15-dmqueue-help-exploration.md
source_section: SUMMARY.md §2 "/dashboard/exploration/transition-to-combat doesn't transition"
status: completed
attempts: 0
---

## Finding (verbatim from review)
`/dashboard/exploration/transition-to-combat` doesn't transition. Returns merged positions JSON but never calls `UpdateEncounterMode` or starts an encounter. Endpoint name overstates the behavior.

## Acceptance criteria
- The endpoint calls `UpdateEncounterMode` and actually transitions the encounter to combat mode (or starts a new combat encounter, per spec).
- Test: POSTing the endpoint flips the encounter mode in the DB and the response includes the new mode.
- Tests pass; `make cover-check` green.

## Worker plan
- Read the exploration dashboard/service tests to match current request/response shapes.
- Add a red test for POST `/dashboard/exploration/transition-to-combat` that expects DB-backed mode flip to `combat` and response mode.
- Run the targeted exploration test to confirm the new test fails for the missing transition.
- Make the minimal handler/service change to call `UpdateEncounterMode` during transition.
- Run targeted tests, then `go test ./...` and `make cover-check`.
- Update this item with files touched and set status to `needs_review`.

## Files touched
- `internal/exploration/dashboard_test.go` — added POST transition test asserting response mode and stored encounter mode flip to `combat`.
- `internal/exploration/integration_test.go` — added DB-backed POST transition test verifying persisted encounter mode.
- `internal/exploration/service.go` — added `TransitionToCombat` to capture positions and call `UpdateEncounterMode`.
- `internal/exploration/dashboard.go` — switched transition endpoint to use `TransitionToCombat` and return `mode` in JSON.
- `.spec-review/state/items/SR-036.md` — recorded worker plan, files touched, and review status.

## Reviewer notes
- Acceptance criteria met: `/dashboard/exploration/transition-to-combat` now calls `Service.TransitionToCombat` from `internal/exploration/dashboard.go:193`, and that service calls `UpdateEncounterMode` with `Mode: "combat"` in `internal/exploration/service.go:219`.
- Response includes the new mode: handler writes `"mode": result.Encounter.Mode` alongside merged positions at `internal/exploration/dashboard.go:204`.
- DB-backed mode flip coverage exists: `TestIntegration_DashboardTransitionToCombatFlipsModeInDB` posts the endpoint, asserts JSON mode `"combat"`, and reloads the stored encounter mode at `internal/exploration/integration_test.go:84`.
- Handler/unit coverage exists: `TestDashboardHandler_TransitionToCombat_FlipsEncounterMode` asserts response mode, stored fake encounter mode, and one `UpdateEncounterMode` call at `internal/exploration/dashboard_test.go:261`.
- Relevant spec satisfied: map-based exploration says combat starts on the current map and adds turn structure (`docs/dnd-async-discord-spec.md:2738`); the implementation now flips the encounter from exploration to combat while preserving captured positions for the combat handoff.
- Regression grep found no stale transition-to-combat handler path returning capture-only merged positions; remaining `CapturePositions` references are helper usage or tests, and the registered route reaches `HandleTransitionToCombat`.
- CLAUDE.md rules checked: TDD/coverage expectations are represented by added tests and the required coverage gate.
- `go test ./...` passed.
- `make cover-check` passed; coverage checker reported 91.95% post-exclusion overall and `internal/exploration` at 90.64%.
- VERDICT: approved
