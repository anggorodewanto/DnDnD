---
id: SR-045
title: Material-component substitution string-match brittle
severity: medium
source_batch: batch-09-spells.md
source_section: SUMMARY.md §3 "Material-component substitution string-match brittle"
status: completed
attempts: 0
---

## Finding (verbatim from review)
Material-component substitution string-match brittle.

## Acceptance criteria
- Material-component substitution uses a structured comparison (component ID / canonical name) rather than fragile string matching.
- Test: a spell whose material component is "a pinch of sand" accepts a hand-sized component pouch or focus correctly across casing/whitespace variants.
- Tests pass; `make cover-check` green.

## Worker plan
- Add `normalizeComponentName` helper that lowercases, strips leading articles (a/an/the), strips "worth (at least) N gp" suffixes, and trims whitespace.
- Replace `strings.EqualFold(item.Name, desc)` with a `matchesMaterialComponent` function that normalizes both sides and checks if the normalized item name contains or is contained within the normalized description.
- Write a failing test: spell with material "a pinch of sand" (no gold cost) auto-proceeds (already works, but verify). Spell with costly material "a diamond worth at least 300 gp" matches inventory items named "diamond", "Diamond", "  a diamond  ", "A Diamond Worth 300gp".
- Make the test pass with minimal code change.
- Run `go test ./...` and `make cover-check`.

## Files touched
- `internal/combat/spellcasting.go` — Added `normalizeComponentName` and `matchesMaterialComponent` helpers; replaced brittle `strings.EqualFold` in `ValidateMaterialComponent` and `RemoveInventoryItem` with normalized matching.
- `internal/combat/spellcasting_test.go` — Added `TestValidateMaterialComponent_NormalizedMatching` covering canonical name, casing, articles, whitespace, and full-description variants.

## Reviewer notes
- `normalizeComponentName` (spellcasting.go:1374) uses structured normalization: lowercases, strips articles ("a"/"an"/"the"), strips "worth …" and ", which …" suffixes, trims whitespace. Not fragile string matching. ✅
- `matchesMaterialComponent` (spellcasting.go:1398) performs bidirectional `strings.Contains` on normalized forms — handles canonical name, full description, and partial matches. ✅
- `ValidateMaterialComponent` (spellcasting.go:1413) and `RemoveInventoryItem` (spellcasting.go:1459) both use `matchesMaterialComponent` instead of the old `strings.EqualFold`. ✅
- Spec compliance: non-costly materials auto-proceed (MaterialCostGp null → MaterialCheckProceed at line 1415). Costly materials require exact item in inventory or gold fallback. Component pouch/focus bypass is implicit (no check when no gold cost). ✅
- Test `TestValidateMaterialComponent_NormalizedMatching` covers: canonical name, uppercase, leading article, whitespace variants, full description, partial with article+caps (happy paths), and "ruby" as unrelated item (negative path). ✅
- `go test ./...` passes (all packages OK). `make cover-check` passes ("OK: coverage thresholds met"). ✅
- No other callers of old `strings.EqualFold(item.Name, desc)` pattern found in non-test Go files. No regressions. ✅
- Code style: early-return in `normalizeComponentName` and `matchesMaterialComponent`, no dead code, no over-engineering. `removed` flag in `RemoveInventoryItem` prevents double-removal — minimal and correct. ✅

VERDICT: approved
